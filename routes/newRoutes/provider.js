// const router = require("express").Router();
// const { updateProvider, updateProviderStatus } = require("../../controllers/newControllers/provider");
// const { tokenVerify, adminTokenVerify } = require("../../common/tokenVerify");

// router.put("/provider/:providerId", tokenVerify, updateProvider);
// router.put("/status/:providerId", adminTokenVerify, updateProviderStatus);
// module.exports = router;
